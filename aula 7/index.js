// * mesmas regras que para as variaveis com let
// ! DIFRENÇA: constantes não podem ser redeclaradas e não podem ter o valor alterado e precisam ser inicializadas com um valor

const nome = "João"; 
const sobrenome = "Silva"
const nomeCompleto = `${nome} ${sobrenome}`;
console.log(nomeCompleto);

